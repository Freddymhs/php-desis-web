{
  "name": "php-watch",
  "version": "1.0.0",
  "scripts": {
    "dev": "php -S localhost:8000 > /dev/null 2>&1 & browser-sync start --proxy 'localhost:8000' --files '**/*.php, **/*.css, **/*.js'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
